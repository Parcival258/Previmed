<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🌐 Permisos -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 📷 La cámara no es obligatoria -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- 🖼️ Permiso moderno para acceder a imágenes (reemplaza READ/WRITE_EXTERNAL_STORAGE) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- 🔔 Permiso para notificaciones (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Previmed"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- 🧩 Actividades del asesor -->
        <activity android:name=".controller.asesor.ContratoActivity" />
        <activity android:name=".controller.asesor.fragmentAsesor.BuscarContratoAsesor" />
        <activity android:name=".controller.asesor.Barrios" />
        <activity android:name=".controller.asesor.AgregarPaciente" />
        <activity android:name=".controller.asesor.PacienteVer" />
        <activity android:name=".controller.asesor.PlanesView" />
        <activity android:name=".controller.asesor.AsociarBeneficiario" />
        <activity android:name=".view.pagos.PagosAdd" />
        <activity android:name=".controller.asesor.PagosView" />
        <activity android:name=".controller.asesor.RegistrarPagoActivity" />

        <!-- 🧍 Actividades de pacientes -->
        <activity android:name=".controller.pacientes.Beneficiarios" />
        <activity android:name=".controller.pacientes.ViewBeneficiario" />
        <activity android:name=".controller.pacientes.SolicitarVisita" />

        <!-- ⚕️ Actividades del médico -->
        <activity android:name=".controller.medico.HistorialVisitas" />
        <activity android:name=".controller.medico.VisitasPendientes" />
        <activity android:name=".controller.medico.Visitas"/>
        <activity android:name=".ViewMedico"/>

        <!-- 🔑 Otras actividades -->
        <activity android:name=".controller.asesor.ViewAsesor" />
        <!-- ❌ Eliminado el error de .Menu (no existe esa clase) -->
        <activity android:name=".controller.Login" />
        <activity android:name=".RegistroUsuario" />

        <!-- 🚀 Actividad principal -->
        <activity
            android:name=".controller.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 📁 FileProvider (para guardar fotos y archivos) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
